# runs lint and the test suite on every push and pr.
# matrix brackets the supported range: 3.10 is the floor (and the only
# version that exercises the tomli fallback), 3.14 is the newest

name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  # dedicated linting job that runs exactly once
  # one modern python version is sufficient, so we use the latest
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"
      - run: python -m pip install ruff
      - run: ruff check src/ tests/
      - run: ruff format --check src/ tests/
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m pip install -e ".[dev]"
      - run: python -m pytest -q
